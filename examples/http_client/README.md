# http_client

Simple `yam-http` client example against free public JSON APIs (`jsonplaceholder.typicode.com`).

## Run

```bash
cargo run -p http_client
# or from this directory:
cargo run
```

Requires internet access.
